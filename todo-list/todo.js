//use an array to model todo list
var todos = ["Buy New Turtle"];

//ask user for input
var input = prompt("What would you like to do?");

//check what input is
if(input === "list") {
  console.log(todos);
}
